# Agent Instructions

- When searching, navigating, or reading code in this project, use Serena's semantic tools first.
- Prefer Serena symbol/project search over raw text search for code understanding, references, and cross-file navigation.
- Use plain shell search such as `rg` only when Serena is unavailable, not connected, or the task is a simple non-code text/file lookup.
