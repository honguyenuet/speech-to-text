{
  "permissions": {
    "allow": [
      "Bash(PGPASSWORD=amoniac123 psql -h localhost -U postgres -d golden_voice -c \"ALTER TABLE users ADD COLUMN IF NOT EXISTS avatar TEXT;\")",
      "Bash(npm install *)",
      "Bash(PGPASSWORD=amoniac123 psql -h localhost -U postgres -d golden_voice -c \"CREATE TABLE IF NOT EXISTS transcriptions \\(id SERIAL PRIMARY KEY, user_id INTEGER REFERENCES users\\(id\\) ON DELETE CASCADE, filename VARCHAR\\(500\\) NOT NULL, file_size INTEGER, duration FLOAT, text TEXT NOT NULL, created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW\\(\\)\\);\")",
      "Bash(npm uninstall *)",
      "Bash(npx tsc *)"
    ]
  }
}
